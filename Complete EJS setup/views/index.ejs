<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/style.css">
    <title>EJS Complete setup</title>
</head>

<body>
    <!-- Display variable from backend -->
    <h1>
        <%= name %>
            <!-- Don't forget to add = sign for the variable -->
    </h1>

    <!-- Conditional Rendering -->
    <% if(age <=17){ %>
        <h3>Not Eligible to open this <%= age %> age</h3>
        <% } else if(age>= 18){ %>
            <h3>You are allowed to pass with this <%= age %> age</h3>
            <% } else{ %>
                <h3>Nothing at all.</h3>
                <% } %>

                    <!-- For loops -->
                    <% for(let i=0; i<3;i++){ %>
                        <li>
                            <%=i+1%>
                        </li>
                        <% } %>

                            <!-- For in loop -->
                            <% const family=["Vishnu", "Vasanth" ,"Jaya"] %>
                                <h1>Family</h1>
                                <% for(data in family){ %>
                                    <li>
                                        <%= family[data] %>
                                    </li>
                                    <% } %>

</body>

</html>